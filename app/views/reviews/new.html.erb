
<h1>New review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.select :rating, (1..5)  %>
  <%# <%= f.options_for_select(CATEGORIES) %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
